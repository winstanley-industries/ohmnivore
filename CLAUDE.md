# Ohmnivore
Ohmnivore is a GPU Accelerated circuit simulation solver

## Conventions

### Dependencies
- Always verify dependency versions are latest stable before adding them: `cargo search <crate> --limit 1`
- Use major version specifiers (e.g., `nom = "8"`) unless a specific minor/patch is needed
